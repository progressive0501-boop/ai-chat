# AI チャットボット 仕様書

## プロジェクト概要

一般ユーザー向けの雑談・雑チャットを楽しめるエンターテインメント系 AI チャットボット。
明るく盛り上がる会話を提供し、ユーザーが気軽に楽しめる Web アプリとして提供する。

---

## 機能要件

### コア機能
- ユーザーがテキストを入力すると、Claude API を通じて AI が応答する
- 明るく盛り上がるキャラクターとして振る舞い、雑談・雑チャットを楽しめる
- 会話履歴はセッション中のみ保持し、ブラウザを閉じるとリセットされる

### 非機能要件
- 認証・ログイン不要（誰でもすぐ使える）
- 日本語のみ対応
- レスポンシブ対応（スマートフォン含む）

---

## 技術スタック

| 項目 | 技術 |
|---|---|
| フレームワーク | Next.js（App Router） |
| API サーバー | Hono |
| ORM | Prisma |
| AI エージェント | Mastra |
| AI モデル | Claude API（Anthropic） |
| デプロイ | Vercel |
| 言語 | TypeScript |

---

## アーキテクチャ

```
ユーザー（ブラウザ）
  ↓ HTTP / SSE
Next.js App Router（フロントエンド + APIルート）
  ↓ Hono（APIハンドラー）
  ↓ Mastra（AIエージェント管理）
  ↓ Claude API（Anthropic）
```

- フロントエンドとバックエンドは **Next.js App Router** で統一
- API エンドポイントは **Hono** で実装（`/api/*`）
- AI エージェントの管理・プロンプト制御は **Mastra** で行う
- データ永続化が必要な場合は **Prisma** 経由でDBアクセス

---

## ディレクトリ構成（推奨）

```
src/
├── app/
│   ├── page.tsx          # チャット画面
│   ├── layout.tsx
│   └── api/
│       └── [[...route]]/
│           └── route.ts  # Hono ルーターのエントリーポイント
├── components/
│   ├── Chat.tsx          # チャット UI コンポーネント
│   ├── MessageList.tsx
│   └── MessageInput.tsx
├── mastra/
│   ├── index.ts          # Mastra インスタンス初期化
│   ├── agents/
│   │   └── chatAgent.ts  # 雑談チャットエージェント定義
│   └── tools/            # 必要に応じてツール追加
├── lib/
│   └── hono.ts           # Hono アプリ定義
└── prisma/
    └── schema.prisma
```

---

## AI エージェント仕様（Mastra）

### キャラクター設定
- **トーン**: 明るく盛り上がる、フレンドリー、テンポ良い
- **言語**: 日本語のみ
- **ペルソナ**: 気軽に話しかけられる友人のような存在

### システムプロンプト例
```
あなたは明るくて元気な雑談チャットボットです。
ユーザーと楽しく盛り上がる会話をすることが得意です。
日本語で話してください。
テンポよく、親しみやすいトーンで応答してください。
```

### 会話履歴
- セッション中のみ保持（クライアントサイドの state で管理）
- ページリロードまたはブラウザを閉じるとリセット

---

## UI 仕様

### チャット画面
- メッセージ一覧表示（自分の発言・AI の応答を区別して表示）
- テキスト入力フォーム（Enter で送信、Shift+Enter で改行）
- AI 応答中はローディング表示
- ストリーミングレスポンス対応（逐次表示）

### デザイン方針
- シンプルで直感的な UI
- スマートフォン・PC 両対応（レスポンシブ）

---

## 環境変数

```env
ANTHROPIC_API_KEY=       # Claude API キー
```

---

## 開発・デプロイ

### ローカル開発
```bash
npm install
npm run dev
```

### デプロイ
- **Vercel** へデプロイ
- 環境変数 `ANTHROPIC_API_KEY` を Vercel の環境変数に設定する

---

## 今後の拡張候補（スコープ外）

- ユーザー認証・会話履歴の永続化
- キャラクター選択機能
- 多言語対応
- 音声入力・読み上げ機能
